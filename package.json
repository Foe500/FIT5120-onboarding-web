{
  "name": "hush-sensory-navigation",
  "version": "1.0.0",
  "private": true,
  "description": "FIT5120 onboarding prototype for sensory-aware Melbourne CBD route comparison.",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "check": "npm run build && node --check backend/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
